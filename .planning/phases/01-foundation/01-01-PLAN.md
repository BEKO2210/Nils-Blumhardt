---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, css/styles.css, css/variables.css]
autonomous: true

must_haves:
  truths:
    - "Website loads and displays all sections (Hero, Leistungen, Galerie, Bewertungen, Kontakt)"
    - "Layout is responsive and looks good on mobile (375px)"
    - "Blue accent color is consistently implemented"
    - "Logo is visible in the header"
  artifacts:
    - path: "index.html"
      provides: "Complete HTML structure with all sections"
      contains: "section"
    - path: "css/styles.css"
      provides: "Mobile-first responsive styles"
      min_lines: 100
    - path: "css/variables.css"
      provides: "CSS custom properties for design system"
      contains: "--color-primary"
  key_links:
    - from: "index.html"
      to: "css/styles.css"
      via: "link rel stylesheet"
      pattern: "link.*styles\\.css"
    - from: "index.html"
      to: "css/variables.css"
      via: "link rel stylesheet"
      pattern: "link.*variables\\.css"
---

<objective>
Create complete HTML structure and mobile-first CSS design system for Blumhardt Maler one-pager.

Purpose: Establish the foundation for the entire website with semantic HTML structure and a consistent visual design system.
Output: Working one-pager with all sections visible and responsive layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Business data for content:
- Inhaber: Nils Blumhardt
- Adresse: Westenfeldstraße 59, 71691 Freiberg am Neckar
- Telefon: 07141 72496
- Mobil: 0160 5972245
- E-Mail: N.Blumhardt@t-online.de
- Öffnungszeiten: Mo-Do 07:00-17:00, Fr 07:00-13:00

Services (for Leistungen section):
- Malerarbeiten Innen & Außen
- Fassadenanstrich
- Tapezieren aller Wandbeläge
- Bodenbelagsarbeiten
- Dekorative Innenputze (Struktur-, Glimmer-, Modellierputze)
- Kreative Oberflächengestaltung (Wischtechnik, Spachteltechnik)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HTML structure with all sections</name>
  <files>index.html</files>
  <action>
    Create semantic HTML5 structure with:

    1. DOCTYPE, html lang="de", proper meta tags (charset, viewport, description)
    2. Head: Title "Nils Blumhardt Maler- und Lackierarbeiten | Freiberg am Neckar", link to CSS files
    3. Header: Logo placeholder (text for now), navigation with anchor links to sections
    4. Hero section (#hero): Headline, subheadline (Malermeister in Freiberg), CTA buttons
    5. Leistungen section (#leistungen): Grid of 6 service cards with icons (use emoji placeholders)
    6. Galerie section (#galerie): Container for 6-8 placeholder image slots
    7. Bewertungen section (#bewertungen): Static Google reviews display (5.0 stars, 4 reviews, placeholder review cards)
    8. Kontakt section (#kontakt): Form placeholder (fields: Name, Email, Telefon, Nachricht), contact info sidebar
    9. Footer: Address, phone, email, copyright

    Use BEM-style class naming (block__element--modifier).
    Add aria-labels for accessibility.
    Include placeholder text in German.
  </action>
  <verify>Open index.html in browser - all sections visible, no console errors</verify>
  <done>HTML file with all 5 main sections (Hero, Leistungen, Galerie, Bewertungen, Kontakt) plus header/footer</done>
</task>

<task type="auto">
  <name>Task 2: Create CSS design system with variables</name>
  <files>css/variables.css, css/styles.css</files>
  <action>
    Create css/ directory and two files:

    **css/variables.css:**
    - Colors: --color-primary (blue accent #2563eb), --color-primary-dark, --color-text, --color-text-light, --color-bg, --color-bg-alt
    - Typography: --font-family (system font stack), --font-size-base, --font-size-lg, --font-size-xl, --font-size-2xl, --font-size-3xl
    - Spacing: --spacing-xs through --spacing-3xl (4px to 64px scale)
    - Borders: --radius-sm, --radius-md, --radius-lg
    - Shadows: --shadow-sm, --shadow-md, --shadow-lg
    - Breakpoints as comments (reference only): 375px mobile, 768px tablet, 1024px desktop

    **css/styles.css:**
    - CSS reset (box-sizing, margin/padding reset, img max-width)
    - Base styles: body font, html scroll-behavior smooth
    - Container: max-width 1200px, centered, padding
    - Section base: padding vertical using spacing variables
    - Header: sticky, white bg, shadow, flex layout
    - Hero: centered text, gradient or solid background, min-height 80vh on mobile
    - Service cards: grid (1 col mobile, 2 col tablet, 3 col desktop)
    - Gallery grid: 2 col mobile, 3 col tablet, 4 col desktop
    - Reviews: card layout with star rating
    - Contact: form styling (inputs, button), info sidebar
    - Footer: dark bg, light text, centered

    Mobile-first: Start with mobile styles, use min-width media queries for larger screens.
    Use CSS Grid and Flexbox - no floats.
  </action>
  <verify>Refresh browser - styles applied, blue accent color visible, layout responds to window resize</verify>
  <done>CSS variables defined, all sections styled, responsive breakpoints working at 375px, 768px, 1024px</done>
</task>

<task type="auto">
  <name>Task 3: Refine responsive layout and visual polish</name>
  <files>css/styles.css, index.html</files>
  <action>
    Polish the visual design:

    1. Navigation: Hide on mobile (hamburger placeholder), show horizontal on desktop
    2. Hero: Adjust typography scale for mobile vs desktop, ensure CTA buttons are prominent
    3. Service cards: Add hover effect (subtle shadow or scale), ensure icons/emoji centered
    4. Gallery: Square aspect ratio containers, placeholder bg color
    5. Reviews: Star icons using Unicode (★), review text truncation if needed
    6. Contact form: Full-width inputs on mobile, side-by-side on desktop where appropriate
    7. Footer: Stack columns on mobile, row on desktop
    8. Add smooth scroll behavior for anchor links
    9. Ensure no horizontal scroll on any viewport
    10. Add focus states for accessibility (visible focus rings on interactive elements)

    Test viewports: 375px (mobile), 768px (tablet), 1024px+ (desktop)
  </action>
  <verify>Test at 375px width - no horizontal scroll, all content readable, touch targets min 44px</verify>
  <done>Website looks polished on all viewport sizes, navigation works, no layout bugs</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] index.html opens without errors in browser
- [ ] All 5 sections visible: Hero, Leistungen, Galerie, Bewertungen, Kontakt
- [ ] Blue accent color (#2563eb or similar) used for buttons, links, accents
- [ ] Layout responds correctly at 375px, 768px, 1024px widths
- [ ] No horizontal scrollbar at any viewport size
- [ ] Logo placeholder visible in header
- [ ] Navigation links scroll to correct sections
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Website displays correctly on mobile-first responsive layout
- CSS design system established with reusable variables
- Foundation ready for Phase 2 features (form, gallery lightbox, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
